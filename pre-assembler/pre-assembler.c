#include <stdio.h>
#include "lib/table.h"
#define MAXLINE 81

int main(int argc, char *argv[]){
    char line[MAXLINE];
    if(argc < 2){
        fprintf(stderr, "pre-assembler error: no given files");
    }
}